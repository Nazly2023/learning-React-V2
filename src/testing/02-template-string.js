const name = "Nazly";
const lastName = "Zambrano";

// Concatenar de manera tradicional
const completeName = name + "" + lastName;

// Concatenando con template string
const completeName1 = `${name}${lastName}`;

console.log(completeName, completeName1);

function getGreeting(nom) {
  return "Hello" + nombre;
}

console.log(`Este es un texto: ${getGreeting(nom)}`);
